/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 *
 * Generated: bun run generate-wrangler:local → wrangler-dev.jsonc; generate-wrangler:prod → wrangler-prod.jsonc
 */
{
	"$schema": "../../node_modules/wrangler/config-schema.json",
	"name": "{{WORKER_NAME}}",
	{{WORKERS_DEV_BLOCK}}
	"compatibility_date": "2025-12-17",
	"compatibility_flags": ["nodejs_compat"],
	"main": "./workers/app.ts",
	"vars": {
		"VALUE_FROM_CLOUDFLARE": "$VALUE_FROM_CLOUDFLARE"
	},
	"observability": {
		"enabled": true,
		"logs": {
			"enabled": true
		}
	},
	"placement": {
		"mode": "smart"
	},
	"durable_objects": {
		"bindings": [
			{
				"name": "{{ pascalCase name }}",
				"class_name": "{{ pascalCase name }}"
			}
		]
	},
	"migrations": [
		{
			"tag": "v1",
			"new_sqlite_classes": ["{{ pascalCase name }}"]
		}
	]
}
