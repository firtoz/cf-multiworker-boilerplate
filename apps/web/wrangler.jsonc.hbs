/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 *
 * Generated: bun run generate-wrangler:local → wrangler-dev.jsonc; generate-wrangler:prod → wrangler-prod.jsonc
 */
{
	"$schema": "../../node_modules/wrangler/config-schema.json",
	"name": "{{WORKER_NAME}}",
	{{WORKERS_DEV_BLOCK}}
	"compatibility_date": "2026-04-17",
	"compatibility_flags": ["nodejs_compat"],
	"main": "./workers/app.ts",
	/* Declares secret bindings for deploy + wrangler types without relying on .env (CI-friendly). See https://developers.cloudflare.com/changelog/post/2026-03-24-secrets-config-property/ */
	"secrets": {
		"required": [
			"SESSION_SECRET",
		]
	},
	"observability": {
		"enabled": true,
		"logs": {
			"enabled": true
		}
	},
	"placement": {
		"mode": "smart"
	},
	"durable_objects": {
		"bindings": [
			{
				"name": "ExampleDo",
				"class_name": "ExampleDo",
				"script_name": "{{EXAMPLE_DO_WORKER_NAME}}"
			},
			{
				"name": "CoordinatorDo",
				"class_name": "CoordinatorDo",
				"script_name": "{{COORDINATOR_DO_WORKER_NAME}}"
			}
		]
	}
}
