# Optional documentation for `apps/web/.env` — not read by scripts.
# Prefer repo-root `/.env.local` for dev; see root `/.env.example`.
# If you use a local `.env` beside apps/web, copy from here and fill values:
#   cp .env.example .env
