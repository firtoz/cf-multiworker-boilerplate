.DS_Store
        *.tsbuildinfo

# React Router
/.react-router/
/build/

# Cloudflare
.mf
.wrangler

# Env — only `.env.local` / `.env.production` for overrides; never plain `.env`
.env
.env.local
.env.production
!.env.example

# Turbo
.turbo
